<?xml version="1.0" encoding="utf-8"?>
<TabHost xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/tabhost"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical" >

        <FrameLayout
            android:id="@android:id/tabcontent"
            android:layout_width="fill_parent"
            android:layout_height="0.0dip"
            android:layout_weight="1.0" />

        <TabWidget
            android:id="@android:id/tabs"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_weight="0.0"
            android:visibility="gone" />

        <RadioGroup
            android:id="@+id/main_radio"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="left"
            android:gravity="left"
            android:orientation="horizontal" >

            <RadioButton
                android:id="@+id/mainTabs_radio_earnings"
                style="@style/mainTabs_radio"
                android:button="@null"
                android:drawableTop="@drawable/earnings_btn"
                android:text="@string/earnings"
                android:textColor="@drawable/tabhost_text_color"
                android:checked="false"/>

            <RadioButton
                android:id="@+id/mainTabs_radio_shop"
                style="@style/mainTabs_radio"
                android:button="@null"
                android:drawableTop="@drawable/shop_btn"
                android:text="@string/shop"
                android:textColor="@drawable/tabhost_text_color" />

            <RadioButton
                android:id="@+id/mainTabs_radio_invitation"
                style="@style/mainTabs_radio"
                android:button="@null"
                android:drawableTop="@drawable/invitation_btn"
                android:text="@string/invitation"
                android:textColor="@drawable/tabhost_text_color" />

            <RadioButton
                android:id="@+id/mainTabs_radio_more"
                style="@style/mainTabs_radio"
                android:button="@null"
                android:drawableTop="@drawable/more_btn"
                android:text="@string/more"
                android:textColor="@drawable/tabhost_text_color" />
        </RadioGroup>
    </LinearLayout>

</TabHost>